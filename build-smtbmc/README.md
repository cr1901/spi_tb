This directory is used for the `yosys-smtbmc` flow of this repository.

The `sby` flow directory `build-sby` is created and removed automatically when
`make sby` and `make clean-sby` respectively are run.
